# Caché de importación de Godot: se regenera con `godot --headless --path . --import`
.godot/
*.import
# Binarios de salida (el APK pesa 130 MB)
export/

# Ajustes personales de Claude Code (permisos locales): no se comparten
.claude/settings.local.json
